# .NET specific - build outputs should not be committed
bin/
obj/
**/*.user
**/*.suo

# Visual Studio
.vs/
*.sln.iml

# Publish output directory (published executables)
published/

# InnoSetup installer script IS tracked - it defines the deployment config
; NOTE: Setup.iss is kept out of gitignore so installer config is version-controlled
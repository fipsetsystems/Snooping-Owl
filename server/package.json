{
  "name": "snoopingowl-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SnoopingOwl agent gateway: WebSocket endpoint + HTTP API (Fastify, TypeScript)",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/websocket": "^11.0.1",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.1",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
